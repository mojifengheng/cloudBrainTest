(1)输入账号密码时，没有处理空格的情况，所以不能够多输入空格
(2)参数中，对于目前的台式机，参数col-xl确定
(3)RF执行完成后，会生成三个文件，分别是log.html, output.xml, report.html,所以得到结果的方式暂定为两种
    |-- 可以直接通过网页加载log.html 或者 report.html
    |-- 可以通过XML解析工具，解析相关的元素，然后给web显示（倾向）
    |-- 还可以告知文件位置，下载文件查看
